<style>
  /* Navbar Styles */
.navbar {
  background-color: #1976d2; /* Blue background for navbar */
  padding: 15px 30px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  position: sticky;
  top: 0; /* Sticks the navbar to the top */
  z-index: 1000; /* Ensures navbar stays on top of other elements */
}

.navbar-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}

.navbar-brand {
  color: white;
  font-size: 2rem;
  font-weight: bold;
  text-decoration: none;
  transition: color 0.3s ease;
}

.navbar-brand:hover {
  color: #e3f2fd; /* Light blue on hover */
}

.navbar-links {
  list-style-type: none;
  margin: 0;
  padding: 0;
  display: flex;
  gap: 20px;
}

.navbar-links li {
  display: inline-block;
}

.navbar-links a {
  color: white;
  font-size: 1.2rem;
  text-decoration: none;
  font-weight: 500;
  padding: 8px 12px;
  border-radius: 5px;
  transition: color 0.3s ease, background-color 0.3s ease;
}

.navbar-links a:hover {
  color: #ffffff;
  background-color: #004ba0; /* Darker blue on hover */
}

/* Navbar Mobile Responsive Styling */
@media (max-width: 768px) {
  .navbar-container {
    flex-direction: column;
    align-items: flex-start;
  }

  .navbar-links {
    flex-direction: column;
    width: 100%;
    gap: 10px;
    margin-top: 10px;
  }

  .navbar-links a {
    text-align: center;
    width: 100%;
  }
}

  </style>
<nav class="navbar">
  <div class="navbar-container">
    <a href="/" class="navbar-brand">Messages</a>
    <ul class="navbar-links">
      <li><a href="/">Home</a></li>
      <li><a href="/messages/new">New Message</a></li>
    </ul>
  </div>
</nav>
